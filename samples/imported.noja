
some_text = "hello!";

list = {"head": 0, "tail": 0, "count": 0};

iter_no = 0;

while list["count"] < 10 {

	item = {"value": list["count"], "next": 0};

	if 0 == list["head"] {

		list["head"] = item;

	} else {

		list["tail"]["next"] = item;
	}

	list["tail"] = item;
	list["count"] = list["count"] + 1;

	iter_no = iter_no + 1;
}

print(an_undefined_variable);